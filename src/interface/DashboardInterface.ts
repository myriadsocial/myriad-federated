export interface ChartTopPointInterface {
  createdAt: string;
  decimal: number;
  exchangeRate: boolean;
  id: string;
  image: string;
  name: string;
  networkId: string;
  symbol: string;
  updatedAt: string;
  native: boolean;
}
